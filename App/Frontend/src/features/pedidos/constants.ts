/**
 * Pedidos feature constants.
 *
 * Centralised values shared across the pedidos feature to avoid
 * magic numbers scattered through components.
 */

/** Fixed shipping cost in ARS for delivery orders. Pickup orders have no shipping. */
export const COSTO_ENVIO = 50;
