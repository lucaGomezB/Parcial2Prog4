Armaremos un guión para un video de como 20 minutos en donde se explica cada funcionalidad que hemos implementado en el proyecto, con un énfasis en el código Backend. QUiero que además me detalles en el guión el contenido de las diapositivas, qué se dirá y las líneas y archivos en donde está ubicado lo que debo mostrar, además de la pantalla con permisos que debo mostrar en funcionamiento. En el video vamos a hablar de cómo implementamos las capas router -> service -> uow -> repository -> model por dominio, los archivos y funcionalidades core/, Alembic + seed idempotente, como usamos SQLModel correctamente con constraints, soft delete, snapshot y relaciones. Cómo funcionan nuestros UoW, cómo implementamos WebSockets, cómo implementamos MP (aclararé que no lo pudimos probar del todo debido al error raro, con lujo de detalle), WebSocketManager, cómo implementamos Cloudinary, cómo usamos las estadísticas para graficar en el Frontend, cómo implementamos los tests, cómo manejams las páginas de forma feature sliced, cómo usamos Zustand, cómo usamos TanStack Query y cómo hicimos que el diseño sea consistente, con pequeñas inconsistencias como que no dispongamos de mobile first skeleton loaders (si es un pequeño fix que no comprometerá la estabilidad del sistema actual, hay que arreglarlo por favor) y finalmente, que tenga un balance de código Front y Back. NO hace falta que use lenguaje técnico muy avanzado, es más importante que se demuestre nuestro conocimiento aplicado al proyecto.
